Está faltando o .weights no repositorio

crie uma venv

depois

pip install streamlit opencv-python-headless numpy deep_sort_realtime

pip install torch

pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
